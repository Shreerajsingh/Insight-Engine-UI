# Where the API lives. Empty means same-origin, which is what the Vite dev proxy gives
# you — set it only when the dashboard is served from somewhere the API is not.
VITE_API_BASE=

# What the dev proxy forwards /api to.
VITE_PROXY_TARGET=http://localhost:3000
